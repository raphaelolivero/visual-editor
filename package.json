{
  "name": "falastin",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*",
    "packages/adapter-vue/demo"
  ],
  "scripts": {
    "build": "yarn workspaces foreach --all --topological run build",
    "dev": "yarn workspace @initeelab/adapter-vue demo dev"
  },
  "license": "MIT",
  "packageManager": "yarn@4.9.2",
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
